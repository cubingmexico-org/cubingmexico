{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": [
    "CRON_SECRET",
    "WCA_CLIENT_ID",
    "WCA_CLIENT_SECRET",
    "NODE_ENV",
    "DATABASE_URL",
    "UPLOADTHING_TOKEN",
    "API_URL",
    "BETTER_AUTH_URL",
    "BETTER_AUTH_SECRET",
    "BACKEND_URL",
    "SUPERADMIN_WCA_IDS",
    "NEXT_PUBLIC_ANNUAL_SUMMARY_ENABLED"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ],
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "lint": {
      "dependsOn": [
        "^lint"
      ]
    },
    "check-types": {
      "dependsOn": [
        "^check-types"
      ]
    },
    "test": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}